# Blog content

Add one `.md` file per post in this folder. The site reads files at build time.

Use `TEMPLATE.md` as the starting point. Required front matter: `title`, `date`, `category`, `image`, `slug`, and `excerpt`.

The `/blog/editor` page provides a live Markdown preview and downloads the edited file. Place that downloaded file here to publish it.
